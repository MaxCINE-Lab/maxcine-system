{
  "name": "@maxcine/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --local",
    "build": "tsc -b --pretty false",
    "bundle:worker": "wrangler deploy --dry-run --outdir dist --config wrangler.toml",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@maxcine/shared": "file:../../packages/shared",
    "hono": "^4.8.5"
  }
}
