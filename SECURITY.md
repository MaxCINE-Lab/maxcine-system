# Security

## 当前控制

- 密码使用 PBKDF2-SHA-256、210,000 次迭代、随机盐值哈希；不存储明文密码。
- 会话为 8 小时 HMAC 签名令牌，使用 HttpOnly、SameSite=Lax cookie；Worker 每次请求还会检查账户是否有效。
- 登录按哈希化邮箱在 D1 中进行 15 分钟、8 次失败的基础限流。
- 授权在 Worker 内执行，前端菜单隐藏不是安全边界。Vitest 包含 dealer 跨经销商、warehouse 未审批订单、管理员权限提升和状态机测试。
- 所有关键写入以 D1 `batch` 提交，并在同一批次记录审计；库存写入 `inventory_transactions`，触发器同步库存且负库存会中止整批事务。
- SN 全局唯一，并通过订单明细与 shipment 外键约束，避免同一 SN 重复绑定/发货。
- 输入使用 Zod 限制长度、格式与枚举；跨来源写请求仅接受配置的 `APP_ORIGIN`。
- 不提交 `.dev.vars`、生产密钥、真实电话、地址、客户资料或产品 SN。

## 上线前必须完成

1. 采用 Cloudflare Access、企业 SSO 或受审计的邀请流程替代演示账号创建流程。
2. 增加 CSRF token（若 API 与前端跨 origin 使用 cookie）、CSP、速率限制/WAF 和安全日志告警。
3. 配置 D1 备份、恢复演练、密钥轮换、会话撤销列表、管理员双人复核。
4. 对 R2 上传增加签名 URL、病毒扫描、内容嗅探防护、保留期和最小权限策略。
5. 完成渗透测试、依赖漏洞处置、隐私/数据保留评审以及真实邮件 provider 的域名验证。

## 报告问题

请勿在公开 issue 中提交凭据或客户资料。上线后应指定受控的安全报告渠道和响应 SLA。
