# TODO and Known Risks

## 未完成的功能

- 业务界面目前提供完整导航、表单和流程 UI，但并非每个管理页面都已接入列表/新增 API；优先接入订单、库存、用户、经销商与售后真实数据。
- 产品详情、真实下载中心内容、联系方式、隐私政策和服务条款均为经过标记的占位内容，须由业务/法务审核。
- 摄像头扫码仅提供浏览器适配接口与手动输入回退；需要选定并测试 `BarcodeDetector`、企业扫描枪或第三方 SDK。
- `delivered` 需要顺丰或人工签收回调；承运商 API 尚未接入。
- 邮件是 no-op mock；未接 Resend/SES，不会发信。
- R2 上传/下载签名、产品媒体管理、售后附件、库存导入导出与报表尚未实现。
- 用户停用、角色变更、密码重置、会话撤销、邀请流和管理员双人复核尚未实现。

## 风险与需要确认的业务规则

- 订单审核通过时首版即扣减“可用库存”作为预留；取消/拒绝后的库存释放规则尚待业务确认，当前未暴露取消接口。
- 订单价格使用创建时的产品价格快照。税、币种、折扣、付款、运费、部分发货与退货规则未定义，未擅自实现。
- SN 在拣货时创建并绑定，未实现预先入库 SN 批量导入；需要确认真实仓库的 SN 生命周期。
- 测试中使用固定公开的本地演示密码，绝不能用于远程或共享环境。
- `npm audit` 当前报告依赖的高危项；上线前应在锁定的运行环境执行审计并按影响修复/豁免。
- 当前 Codex 本机中，Wrangler/Vite 的默认 Esbuild service 会在打包阶段无输出地挂起（直接对最小空文件执行 Esbuild bundle 也可复现）；前端已通过 Vite 的原生配置加载器构建，Worker 已通过 TypeScript 构建/类型验证，但 `npm run bundle:worker -w @maxcine/api` 需要在标准 CI 或 Cloudflare 环境重新执行并确认产物。

## 后续建议顺序

1. 用真实但脱敏的业务规则确认库存预留、取消、售后和交付回调。
2. 接入测试 Cloudflare 环境、受控 SSO、R2 签名上传和扫描设备。
3. 为 D1 流程添加 Miniflare/D1 集成测试、并发库存测试和端到端 UI 测试。
4. 完成安全、法务、品牌和运维评审后，再制定生产发布与 DNS 切换计划。
